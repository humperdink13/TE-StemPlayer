#!/usr/bin/env python3
"""
Stem Player Serial Protocol Client
Communicates with the custom firmware over CDC ACM serial
"""

import serial
import serial.tools.list_ports
import struct
import sys
import time

CRC8_TABLE = bytes([
    ,
])

def crc8(data):
    crc = 0
    for b in data: crc = CRC8_TABLE[crc ^ b]
    return crc

def cobs_encode(data):
    out = bytearray()
    idx = 0
    while idx <= len(data):
        end = idx
        while end < len(data) and data[end] != 0: end += 1
        out.append(end - idx + 1)
        out.extend(data[idx:end])
        if end < len(data): idx = end + 1
        else: break
    out.append(0x00)
    return bytes(out)

def cobs_decode(raw):
    data = raw[:-1] if raw and raw[-1] == 0 else raw
    out = bytearray()
    idx = 0
    while idx < len(data):
        code = data[idx]; idx += 1
        if code == 0: break
        for _ in range(code - 1):
            if idx < len(data): out.append(data[idx]); idx += 1
        if code < 0xFF and idx < len(data): out.append(0)
    return bytes(out)

class StemPlayerSerial:
    def __init__(self, port_path):
        self.ser = serial.Serial(port_path, 115200, timeout=1)
        self.seq = 1
    
    def send_cmd(self, cmd, payload=b'', timeout=3.0):
        self.ser.reset_input_buffer()
        body = bytes([0x51, self.seq & 0xFF, cmd, len(payload)]) + payload
        pkt = cobs_encode(body + bytes([crc8(body)]))
        self.ser.write(pkt)
        self.seq += 1
        
        deadline = time.time() + timeout
        buf = bytearray()
        while time.time() < deadline:
            if self.ser.in_waiting > 0:
                buf.extend(self.ser.read(self.ser.in_waiting))
                if 0x00 in buf:
                    idx = buf.index(0x00)
                    decoded = cobs_decode(bytes(buf[:idx+1]))
                    if len(decoded) >= 5:
                        plen = decoded[3]
                        return decoded[4:4+plen]
            time.sleep(0.01)
        return None
    
    def status(self):
        r = self.send_cmd(0x52)
        if r and len(r) >= 8:
            ver = struct.unpack_from('<I', r, 0)[0]
            emmc = r[4]
            return {'version': ver, 'emmc_ok': emmc == 1}
        return None
    
    def info(self):
        r = self.send_cmd(0x49)
        if r and len(r) >= 10:
            name = r[0:4].decode('ascii', errors='replace').rstrip('\x00')
            sectors = struct.unpack_from('<I', r, 4)[0]
            sector_size = struct.unpack_from('<H', r, 8)[0]
            return {'name': name, 'total_sectors': sectors, 'sector_size': sector_size}
        return None
    
    def read_sector(self, sector_num):
        r = self.send_cmd(0x53, struct.pack('<I', sector_num), timeout=10)
        # Collect all chunk responses
        if r and len(r) > 4:
            # First chunk
            data = bytearray(8192)
            offset = struct.unpack_from('<H', r, 0)[0]
            chunk_len = struct.unpack_from('<H', r, 2)[0]
            data[offset:offset+chunk_len] = r[4:4+chunk_len]
            # TODO: read remaining chunks
            return bytes(data)
        return None
    
    def close(self):
        self.ser.close()

if __name__ == '__main__':
    port = sys.argv[1] if len(sys.argv) > 1 else None
    if not port:
        for p in serial.tools.list_ports.comports():
            if 'stem' in (p.description or '').lower() or (p.vid and p.vid == 0x2367):
                port = p.device; break
    
    if not port:
        print("No stem player serial port found")
        sys.exit(1)
    
    sp = StemPlayerSerial(port)
    
    print("Status:", sp.status())
    print("Info:", sp.info())
    
    # Read first sector (album metadata)
    data = sp.read_sector(0)
    if data:
        print(f"Sector 0: {data[:64].hex()}")
        if b'ALBUM_PRESENT' in data:
            print("Album found!")
    
    sp.close()
